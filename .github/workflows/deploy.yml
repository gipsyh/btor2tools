name: Deploy

on:
  push:
    branches: ["btorvcd"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Pull submodules
        run: git submodule update --init --recursive
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build docker image
        run: |
          docker build . -t btorsim
      - name: Deploy
        run: |
          docker tag btorsim ghcr.io/${{ github.repository_owner }}/btorsim:latest
          docker push ghcr.io/${{ github.repository_owner }}/btorsim:latest
